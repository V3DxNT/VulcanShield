-- VulcanShield demo seed is intentionally disabled by default.
-- This keeps the database in a clean, empty state until a simulation or explicit
-- demo seed is intentionally triggered.
--
-- To opt back into seeded demo data, run the SQL from database/seed/seed.sql
-- manually after connecting to the database.
SELECT 1 WHERE FALSE;
