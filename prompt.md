The UI of the system is fantastic

We now to focus on the Mathematics of the system,

Right now the Extracted Evidence signal is showing wrong info
Check if the ollama model is working or not! 
Will it work if I restart the docker container?

And in the network graph, a lot of users are getting connected to one device and one IP which is practically wrong!
This has to be a problem in the generation of the datas, check it from there! 

The XGBoost and Isolation Forest should both show their outputs in it! Right now only the clicked one is showing it!

In the Ai Confidence score it says llama 3.3 which is wrong, all this should be dynamic.

Make sure that the rag retrieval trace is actually correct,
and try to minimize the ai hallucination here

A Customer had a transaction of 300(success) but still his max amount is shown as 250,
Make sure the entire functionality working fine 

________________

The Graph issue is not resolved